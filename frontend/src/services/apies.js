const BASE_URL = 'http://localhost:8000/api/';

export const endpoints = {
                      
  SIGNUP_API: BASE_URL + "register",               // No change: already correct
  LOGIN_API: BASE_URL + "login",                 // No change: already correct
  LOGOUT_API: BASE_URL + "logout",  
}
